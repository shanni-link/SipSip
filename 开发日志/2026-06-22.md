# 📔 开发日志 — 2026-06-22

---

## ✅ 今日完成

### 项目基础设施
- [x] 创建 `CLAUDE.md` — 项目工作指引（含文件路径、工作流程、禁止事项）
- [x] 创建 `docs/` 文件夹 — 开发规范文档
  - [x] `development-standards.md` — 编码/命名/样式/Git 规范
  - [x] `tech-specs.md` — 技术栈/配置/数据模型/路由设计
  - [x] `design-specs.md` — 设计 Token 速查卡（CSS变量/字体/品牌色/组件尺寸）
  - [x] `execution-steps.md` — 10 阶段执行步骤（0-10，含预估时间）
- [x] 创建 `开发日志/` 文件夹

### 规划阶段回顾
- [x] PRD v1.4 确认
- [x] Architecture v1.0 确认
- [x] Pages v1.0 确认
- [x] Design System v2 (frontend-design 升级)
- [x] 可视化设计 Demo (`design-system-demo.html`)

### Phase 0：项目初始化 ✅
- [x] Vite + React + TypeScript 项目创建
- [x] 依赖安装：react-router-dom / zustand / dexie / framer-motion / exif-js
- [x] Tailwind CSS v4.3 安装 + @tailwindcss/vite 插件配置
- [x] 全局 CSS Token 注入（`src/styles/tokens.css` — @theme 块）
- [x] 纸张纹理全局背景（SVG feTurbulence noise）
- [x] Google Fonts 引入（Noto Serif SC + Zen Kaku Gothic New + Ma Shan Zheng + ZCOOL KuaiLe）
- [x] 全局样式重置 + @keyframes 动画库
- [x] 基础路由框架（HashRouter + 4 页面占位）
- [x] `npm run build` 通过（240KB JS + 17KB CSS）

### Phase 1：Header 组件 ✅
- [x] `MonthHeader` v2 — **重新设计**：去掉左右箭头，改为"今日"+日期显示
  - 当前月：显示「今日」+ 「2026年6月22日 星期X」
  - 历史月：显示「2026年5月」+ 「← 回到今日」
  - 点击日期区域 → 弹出 MonthPicker 年月选择器
  - 月份切换改用日历左右滑动（后续 CalendarGrid 实现）
- [x] `MonthPicker` — 年月选择器弹层（遮罩 + 年份左右切换 + 4×3 月份网格）
  - 当前月有虚线标记，选中月茶色填充
- [x] `ListHeader` — ←返回 + 标题 + 🔍搜索/筛选图标
- [x] `StepHeader` — ←返回 + ●●○○○○ 步骤指示器
- [x] 三端响应式基础（max-width: 500px 居中）
- [x] 挂载到对应页面（CalendarPage / ListPage / NewRecordPage）
- [x] `npm run build` 通过（35 modules）

### Phase 2：Sidebar 组件 ✅
- [x] `BottomNavBar` — 底部导航栏（仅日历页显示）
  - 左侧「📅 日历」标签（始终高亮），点击回到首页
  - 居中 FAB 按钮（🧋 奶茶杯+加号），突出导航栏 18px
  - FAB：56×56px 圆形，印章红底色 + 茶金色发光阴影（shadow-glow）
  - hover 放大 1.05 + 颜色变亮，active 缩小 0.95 + 颜色加深
  - 手绘风 SVG 图标：日历（折角）+ 奶茶杯（加号+奶盖波纹）
  - 适配 iPhone safe area（env(safe-area-inset-bottom)）
  - 最大宽度 500px 居中，与页面同宽
  - 固定在视口底部，z-index: 150
- [x] `FilterPanel` — 筛选浮层（列表页使用）
  - 从底部滑入（slide-up 300ms ease-bounce）
  - 品牌筛选：6 个品牌标签（喜茶/奈雪/蜜雪冰城/茶百道/霸王茶姬/其他），多选
  - 选中标签使用品牌主色填充
  - 评分筛选：印章体数字按钮 1-5 分，选中变印章红
  - 清除全部 / 应用筛选 按钮
  - 点击遮罩关闭
  - 状态标签显示已选筛选条件数量
- [x] 挂载：BottomNavBar → CalendarPage，FilterPanel → ListPage
- [x] `npm run build` 通过（40 modules）

---

## 📋 待办

- [ ] **Phase 3：Card 组件**
  - ReceiptCard（小票卡片 — 核心复用组件）
  - DayCell + TeaThumb + TeaStack（日历格 + 缩略图交叠）

---

## 🚧 阻碍项

- 无

---

## 📝 备注（2026-06-22）

- 开发服务器端口：5178（5173-5177 被占用）
- Tailwind v4 使用 CSS-first 配置（@theme 块），与 v3 的 tailwind.config.js 不同
- Header 组件共享 `Header.css`，通过 CSS 类名隔离
- 所有颜色/字体使用 Tailwind @theme token + CSS 变量双重定义

---

## 📝 备注（2026-06-23）

- Phase 2 Sidebar 组件完成
- BottomNavBar 仅 2 个按钮：日历标签 + FAB，FAB 突出导航栏 18px
- FilterPanel 品牌筛选用真实品牌色填充，评分用印章体数字
- 底部导航使用 `position: fixed`，CalendarPage 需 `padding-bottom: calc(60px + safe-area)` 避免内容被遮挡

### FilterPanel v2 重设计
- [x] **改为下拉面板**：从 ListHeader 下方直接滑出（slide-down 250ms），无遮罩层
- [x] **品牌标签放大**：字号 12px → 15px (`text-base`)，内边距加大（`space-2 space-5`），间距 12px
- [x] **评分统一**：「最低评分」→「评分」，字号 17px → 15px，字体从 stamp → body，与品牌标签一致
- [x] **新增日期筛选**：左右箭头切换年/月 + "本月"快捷 + "不限"清除，默认跟随列表当前月份
- [x] **ListPage 月份选择栏**：头部下方显示"2026年6月"+ 左右箭头切换月份 + 非当前月显示"回到本月"
- [x] `npm run build` 通过（40 modules，CSS 27.6KB + JS 250.3KB）

### 列表页 & FilterPanel v3 精修
- [x] **ListPage 标题放大**：新增 `.header__title--lg`（20px），ListHeader 支持 `titleClassName` prop
- [x] **月份日期缩小**：20px → 17px（`text-md`），与标题形成大小对比（标题 > 日期）
- [x] **月份居中修复**：「回到本月」改为绝对定位（`position: absolute; right`），日期始终居中
- [x] **回到本月按钮美化**：改为「本月」茶色小药丸（bg tea-50 + border + 圆角 md），字重 500
- [x] **FilterPanel 去标题**：移除「筛选」h2 标题 + 已选条件计数，内容直接呈现
- [x] **日期置顶**：筛选面板顺序改为 日期 → 品牌 → 评分
- [x] **年月日三级日期筛选**：年份左右箭头 + 4×3 月份网格（直接点击选月）+ 日 pill 行（−全月/1/5/10/15/20/25/31）
- [x] **分组标题放大**：12px → 15px（`text-base`），字重 600
- [x] **chip 统一样式**：品牌和评分共享 `.filter-panel__chip`，圆角从 full → md（8px），内边距加大（8px 18px），间距 10px
- [x] **评分按钮统一**：与品牌标签同框同字号（13px `text-sm`），选中变印章红
- [x] `npm run build` 通过（40 modules，CSS 27.6KB + JS 251.2KB）

### FilterPanel v4 + 列表页精修
- [x] **本月按钮修复**：从绝对定位改为 flex 内联排列（← 日期 → 本月），不再重叠
- [x] **面板展开动画**：从 `slide-down` keyframe 改为 `max-height` 过渡（280ms ease-out），平滑自然，不再"弹出一片空白"
- [x] **日期递进式选择**：年 → 月 → 天 三步递进
  - 显示行：`[2026年] / [6月] / [−]` 三部分各自可点击
  - 点年 → 年份网格（前后各5年）→ 选完自动跳月份
  - 点月 → 4×3 月份网格 → 选完自动跳天
  - 点天 → 7 列天网格，首格「−」代表全月
- [x] **chip 尺寸调整**：padding `8px 18px` → `5px 14px`，适中不臃肿
- [x] **间距拉大**：分组标题到内容 `12px` → `16px`，最后分组到按钮 `12px` → `20px`
- [x] **按钮缩小**：高度 `44px` → `36px`，字号 `15px` → `13px`（与 chip 一致）
- [x] **清除按钮美化**：从红色文字 → 茶色药丸（与「本月」同款：bg tea-50 + border divider）
- [x] `npm run build` 通过（40 modules，CSS 28.4KB + JS 251.1KB）

### FilterPanel v5 + 列表页精修
- [x] **本月按钮固定居中**：日期 + 本月放入居中 flex 组（`justify-content: center`），不再浮动到最右边，不再与日期重叠
- [x] **面板遮盖下方内容**：FilterPanel 改为绝对定位（`position: absolute`），展开时覆盖月份栏和列表区
- [x] **日期选择改为弹出滚轮**：类似 iOS 闹钟，弹出遮罩 + 三列独立滚动选择器（年/月/日），不挤占原面板空间
  - 日列首项「−」代表全月
  - 每列 `scroll-snap-type: y mandatory`，居中高亮条
  - 确认按钮关闭弹窗
- [x] **日期显示缩小**：字号从 15px → 13px（`text-sm`），padding 从 `6px 12px` → `5px 10px`，与 chip 统一
- [x] **全量间距拉大**：
  - 分组标题 → 内容：16px → 12px（恢复合理间距，日期行小不需要太大）
  - chip 间距：8px → 12px（`space-3`）
  - 分组间距：16px → 20px（`space-5`）
  - 最后分组 → 按钮：20px → 24px（`space-6`）
- [x] **按钮不再撑满**：取消/应用去除 `flex: 1`，改为 `padding: 0 var(--space-5)` 自适应宽度
- [x] `npm run build` 通过（40 modules，CSS 28.6KB + JS 251.4KB）

### FilterPanel v6 + 列表页间距精修
- [x] **本月按钮右移**：gap 8px→12px（`space-3`），日期加 `flexShrink: 0` 固定不动，本月加 `marginLeft: 4px` 微调
- [x] **各行间距拉大**：group `margin-bottom` 20px→**32px**（`space-8`），边框+文字作为整体
- [x] **chip 框间距拉大**：12px→**16px**（`space-4`），框与框之间透气
- [x] **按钮行与上方间距**：`margin-top` 24px→32px（`space-8`），加 `padding-top: 8px`（半行空白）
- [x] **按钮宽度**：padding 20px→16px（`space-4`），`min-width: 4字符`（≈52px），约四字宽
- [x] **按钮字距**：`letter-spacing: 0.5em`（半个字间距），`text-align: center` 居中
- [x] **清除按钮同步**：同 padding/letter-spacing/min-width
- [x] `npm run build` 通过（40 modules，CSS 28.8KB + JS 251.5KB）

### FilterPanel v7 — 月份跳动修复 + 间距大幅拉大
- [x] **月份跳动修复**：本月按钮从条件渲染改为始终渲染，当前月用 `visibility: hidden` 占位 + `disabled`，日期绝对不动
- [x] **chip 框间距**：12px → 16px → **20px**（`space-5`），框与框之间明显透气
- [x] **group 行间距**：20px → 32px → **48px**（`space-12`），边框+文字整体间隔拉大
- [x] **group 标题→内容**：12px → **16px**（`space-4`）
- [x] **按钮行→上方**：24px → 32px → **48px**（`space-12`）+ `padding-top: 12px`，清除不再和 1分连一起
- [x] dev server 重启（端口 5178）
- [x] `npm run build` 通过（40 modules，CSS 28.8KB + JS 251.5KB）
