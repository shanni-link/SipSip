# 🧋 奶茶记录仪 — 产品需求文档 (PRD)

> 版本：v1.4 | 日期：2026-06-22 | 状态：已确认
>
> 📌 本文件为项目需求唯一真相源。后续所有需求变更需同步更新本文档。

---

## 一、产品定位

一款运行在浏览器上的奶茶记录工具（PWA），**移动端优先**，电脑端用于前期测试。核心体验：**拍照记录 → AI 抠图 → 填写信息 → 生成小票 → ✂️撕下小票 → 日历回顾 → 点击听心情**。每一杯奶茶都是一张"消费凭证+回忆存根"。

---

## 二、目标用户 & 使用场景

- **用户画像**：喜欢喝奶茶、想记录打卡的普通用户，主力使用手机
- **核心场景**：
  1. 买了一杯奶茶 → 拍照 → AI 抠图 → 填信息（名称/品牌/门店/口味/价格/评分）→ 录心情 → 生成小票 → ✂️撕下 → 存入日历+列表
  2. 月底回顾 → 打开日历 → 看到哪天喝了什么奶茶 → 点击缩略图 → 展开小票 + 播放语音
  3. 浏览列表 → 翻看一叠小票存根 → 一天多杯时小票叠放 → 左右滑动翻看 → 点击播放语音

---

## 三、功能清单

### 3.1 核心功能（v1 必做）

#### 📸 记录奶茶
| 能力 | 说明 |
|------|------|
| 拍照 / 上传 | 支持相机拍照和相册选择 |
| 日期自动识别 | 读取照片 EXIF 拍摄日期；如无 EXIF 则默认当天 |
| AI 抠图 | 混合方案：默认浏览器本地 AI 模型（免费不限量），不满意可切换 Remove.bg API（专业效果） |
| 奶茶名称 | 具体饮品名（如"多肉葡萄"、"伯牙绝弦"），手动输入 |
| 品牌选择 | 预设常见品牌，支持自行输入 |
| 门店输入 | 手动输入门店名称（如"XX路店") |
| 口味选项 | 甜度 + 温度 + 杯型 + 小料（多选） |
| 价格 | 输入金额（如 ¥18.00） |
| 评分 | ⭐ 1-5 星打分 |
| 心情记录 | 语音输入（优先）+ 文字输入，支持同时保存 |
| ✂️ 小票生成+撕下+贴入日历 | 填写完成 → 生成小票 → 撕下动效 → ①奶茶抠图飞入日历对应日期"贴上" → ②小票飘入右上角收集盒 |

#### 📅 日历视图（月历）

**日历区域**：
- 传统月份格子布局，每个日期格显示当天奶茶抠图缩略图
- 支持月份左右切换
- **📦 右上角小票收集盒图标**：点击打开小票列表（支持筛选），撕下的小票飘入此盒
- **多杯交叠展示**：同一天有多杯奶茶时，缩略图以卡片交叠（错层堆叠）形式展示，模拟照片叠放
- **交互**：点击缩略图 → 展开对应奶茶的完整小票 + 自动播放语音
- 小票点空白处关闭
- 最多展示 3 层交叠，超出显示 "+N" 标记

**📌 贴到日历动效**（新增记录完成后触发）：
- 奶茶抠图从撕下的小票中"飞出"
- 飞向日历对应日期格
- "啪"地贴到格子上（缩放+弹性动效）

**📊 月度统计卡片**（日历下方）：
```
┌──────────────────────────────┐
│  📆 2026年6月                │
│                              │
│  🧋 本月 12 杯  ·  🏪 5 家店 │
│                              │
│  [抠图1] [抠图2] [抠图3]     │  ← 本月奶茶抠图精选（最多5张）
│   [抠图4] [抠图5]            │
│                              │
│  💰 消费 ¥ 186.00           │
└──────────────────────────────┘
```
- 抠图从本月记录中随机选 5 张
- 杯数 = 本月记录总数
- 店数 = 本月去过的不同门店数
- 金额 = 本月消费总额

**🏆 历史最爱**（月度统计下方）：
```
┌──────────────────────────────┐
│  🏆 本月最爱                  │
│                              │
│  [奶茶抠图]                   │
│  多肉葡萄 × 3 杯             │
│                              │
│  📅 查看历史最爱 >           │  ← 点击展开
└──────────────────────────────┘
```
- 自动统计本月喝最多的奶茶（名称+杯数+抠图）
- 点击"查看历史最爱"→ 展开历史各月最爱列表：
  ```
  2026年6月 → 多肉葡萄 × 3杯
  2026年5月 → 伯牙绝弦 × 2杯
  2026年4月 → 杨枝甘露 × 4杯
  ...
  ```
- 可折叠收回

#### 📋 列表视图（小票存根）
- 以**小票/发票样式**展示每杯奶茶记录，像一叠消费存根
- 按日期分组：每天一组
- **同天多杯 → 小票交叠堆叠**：
  - 多张小票以错层重叠方式叠放，模拟桌上一叠收据
  - 每张小票略微偏移露出边缘，可以看到有几张
  - 👆 **左右滑动**翻看同一组内的不同小票
  - 滑动时小票有翻页/切换动效
- **小票内容结构**（统一格式，不同品牌微调配色）
- **品牌差异化**：不同品牌小票配色不同（喜茶=黑金、蜜雪=红白、奈雪=绿白…），内容结构一致
- **交互（唯一操作）**：点击小票 → 播放语音 🔊（小票已展示所有详情，无需二次点击）
- 支持按品牌筛选、按评分排序

#### 🔍 回顾 & 播放
- **唯一交互 = 点击**：点击奶茶小票 → 播放语音（若有录音）
- 小票本身已展示全部信息（抠图/名称/品牌/门店/口味/价格/评分/心情文字），无需二次展开
- 日历中：点击缩略图 → 展开完整小票 + 播放语音
- 列表中：小票已完整可见，点击 → 播放语音
- 编辑 / 删除：小票底部提供小号编辑&删除按钮（次要操作，不干扰主交互）

#### 🎨 小票设计规范

**不同品牌 → 不同配色方案**，但内容结构统一：

| 品牌 | 主色调 | 风格 |
|------|--------|------|
| 喜茶 | 黑金 `#1a1a1a` + `#d4a574` | 极简高端 |
| 奈雪的茶 | 绿白 `#2d5a27` + `#f5f0e8` | 清新自然 |
| 蜜雪冰城 | 红白 `#e60012` + `#ffffff` | 活泼亲民 |
| 茶百道 | 蓝白 `#1a5276` + `#f0f4f8` | 清爽简洁 |
| 霸王茶姬 | 紫金 `#4a235a` + `#f0c27a` | 国风华丽 |
| 其他品牌 | 暖棕 `#5d4037` + `#faf3e0` | 经典茶饮 |

**小票布局**（移动端为主，约 320-360px 宽）：
```
┌─────────────────────────────┐
│ ～～～ 锯齿撕边 ～～～～～～～│  ← 顶部撕边纹理
│                             │
│        🧋 [奶茶抠图]        │  ← 居中展示
│                             │
│     多肉葡萄                │  ← 奶茶名（大号加粗）
│     喜茶 · XX路店           │  ← 品牌 + 门店
│                             │
│     大杯 · 少冰 · 半糖      │  ← 规格行
│     +珍珠 +脆波波           │  ← 小料行
│                             │
│     ⭐⭐⭐⭐☆              │  ← 评分
│                             │
│     ¥ 18.00                │  ← 价格（醒目）
│                             │
│  ───────────────────────    │  ← 虚线分隔
│  "今天超开心！珍珠很Q弹~"    │  ← 心情文字
│                        🔊  │  ← 语音播放
│                             │
│     2026.06.22             │  ← 日期
│ ～～～ 锯齿撕边 ～～～～～～～│  ← 底部撕边纹理
└─────────────────────────────┘
```

**✂️ 撕下动效**：
- 新增记录完成后，展示完整小票
- 用户用手指下滑/点击"撕下"按钮
- 小票从顶部锯齿撕边处"撕裂"分离，带纸张撕裂音效
- 小票缩小飞入列表底部（存入动画）
- 自动返回首页

### 3.2 后期功能（v2+）

| 功能 | 说明 |
|------|------|
| 🗺️ 地图定位门店 | 接入高德/百度地图 API，自动定位展示附近奶茶门店 |
| ☁️ 云端同步 | 账号系统 + 云端存储，换设备数据不丢失 |
| 📊 统计面板 | 本月喝了多少杯、最爱的品牌、甜度偏好等 |
| 🎨 主题皮肤 | 不同风格的主题配色 |

---

## 四、预设数据

### 奶茶品牌（预设，可自定义添加）
喜茶、奈雪的茶、蜜雪冰城、茶百道、古茗、霸王茶姬、一点点、CoCo都可、瑞幸咖啡、星巴克、沪上阿姨、书亦烧仙草、茶颜悦色、LINLEE、茉酸奶

### 口味选项
| 类别 | 选项 | 选择方式 |
|------|------|---------|
| 甜度 | 全糖 / 七分糖 / 半糖 / 三分糖 / 无糖 | 单选 |
| 温度 | 正常冰 / 少冰 / 去冰 / 常温 / 温热 / 热 | 单选 |
| 杯型 | 中杯 / 大杯 / 超大杯 | 单选 |
| 小料 | 珍珠 / 椰果 / 脆波波 / 奶盖 / 芋泥 / 布丁 / 仙草 / 红豆 / 燕麦 / 芝士 / 冰淇淋 / 芦荟 | 多选 |

### 评分
- ⭐ 1-5 星，点击星标打分

### 价格
- 手动输入金额，支持小数点（如 18.00）

### 心情（语音 + 文字）
- 默认文字引导：如"今天喝这杯奶茶的时候，心情怎么样？"
- 语音录制的文字转写结果 + 原始音频同时保存

---

## 五、技术方案概览

| 层级 | 选型 | 说明 |
|------|------|------|
| 前端框架 | React + TypeScript | 生态丰富，PWA 支持好 |
| UI 组件库 | 待定（可选 Tailwind CSS + shadcn/ui 或 Ant Design Mobile） | 保证移动端体验 |
| 数据存储 | IndexedDB（Dexie.js） | 浏览器本地数据库，离线可用 |
| 语音录入 | Web Speech API（SpeechRecognition） | 浏览器原生支持，无需后端 |
| 图片 EXIF | exif-js | 读取照片拍摄日期 |
| AI 抠图（默认） | @imgly/background-removal | 浏览器本地运行，完全免费不限量，效果良好 |
| AI 抠图（增强） | Remove.bg API | 专业效果兜底，用户不满意本地结果时可切换，免费 50 张/月 |
| PWA | Workbox + manifest.json | 手机添加到桌面，离线可用 |
| 地图（v2） | 高德地图 JS API | 定位 + 附近门店搜索 |

---

## 六、数据模型

```
奶茶记录 {
  id: 唯一ID,
  date: 日期（来自EXIF或手动选择）,
  原图: 本地base64/Blob,
  抠图: 去背景后的base64/Blob,
  奶茶名称: 字符串（如"多肉葡萄"）,
  品牌: { 预设名 或 自定义输入 },
  门店: 字符串,
  甜度: 枚举,
  温度: 枚举,
  杯型: 枚举,
  小料: 字符串数组,
  价格: 数字（如 18.00）,
  评分: 数字 1-5,
  心情文字: 字符串（可选）,
  心情语音: base64/Blob（可选）,
  语音转写: 字符串（可选）,
  创建时间: 时间戳,
  更新时间: 时间戳
}
```

---

## 七、页面结构

```
├── 首页 / 日历视图（默认）/
│   ├── 顶部栏：月份切换 + [📦 小票收集盒] 图标（列表唯一入口）
│   ├── 主体：月历网格，每日显示奶茶缩略图（多杯交叠）
│   ├── 📊 月度统计卡片（本月杯数/店数/5张抠图/消费总额）
│   ├── 🏆 历史最爱卡片（本月最爱 → 点击展开历史各月）
│   └── 底部：📅日历 + 🧋FAB "+" 两个按钮
│
├── 列表视图（小票存根）/list
│   ├── 唯一入口：右上角📦收集盒（底部无列表Tab）
│   ├── 顶部：← 返回日历 + 筛选/排序
│   └── 主体：按日期分组，同天多张小票交叠堆叠，左右滑动翻看
│
├── 新增记录页（流程式）/new/*
│   ├── Step 1：拍照/选照片
│   ├── Step 2：确认日期（自动识别 + 手动调整）
│   ├── Step 3：AI 抠图
│   ├── Step 4：填信息（奶茶名/品牌/门店/杯型/甜度/温度/小料/价格/评分）
│   ├── Step 5：录音/输入心情
│   ├── Step 6：小票预览
│   └── Step 7：✂️ 撕下 → 📌 抠图飞入日历贴上 + 小票飘入📦收集盒 → 返回首页
│
└── 小票展开（从日历/列表点击进入）
    ├── 完整小票 + 自动播放语音
    ├── 底部小号：编辑 / 删除
    └── 点空白处关闭
```

---

## 八、状态 & 边界

### 空状态
| 场景 | 处理 |
|------|------|
| 没有任何记录 | 日历显示空白，中间引导"记录你的第一杯奶茶吧 🧋" |
| 某月没有记录 | 该月日历格子全部为空 |
| 照片无 EXIF 日期 | 默认使用当天日期，用户可手动修改 |

### 错误状态
| 场景 | 处理 |
|------|------|
| 本地抠图失败 | 自动切换 Remove.bg API；如也失败则提示"抠图失败，将使用原图展示"，不阻断流程 |
| Remove.bg 额度用完 | 仅使用本地模型，提示用户本月 API 额度已用完 |
| 语音录制失败 | 降级为仅文字输入 |
| 浏览器不支持语音 | 隐藏语音按钮，仅展示文字输入 |
| 存储空间不足 | 提示用户清理旧记录 |

### 极限场景
- 同一天多杯奶茶：
  - **日历**：交叠展示（最多 3 层），超出显示 "+N"
  - **列表**：小票交叠堆叠，左右滑动翻看，超出 3 张时显示滑动指示器
- 大量记录（>500 条）→ 列表分页加载，日历按需渲染

---

## 九、非功能需求

- **PWA 离线可用**：无网络时仍可查看已有记录，新增记录等网络恢复后不影响使用（抠图除外）
- **响应式设计**：手机（<768px）和电脑（≥768px）都有良好体验
- **隐私**：所有数据存储在浏览器本地，不上传至任何服务器（抠图 API 仅上传奶茶图片用于去背景处理）

---

## 十、待确认事项

- [x] ~~抠图方案~~ → 混合方案（本地模型 + Remove.bg 兜底）
- [x] ~~多杯展示~~ → 日历格交叠堆叠，点击不同卡片展开不同详情
- [x] ~~交互模型~~ → 极简：小票=全貌，唯一点击=播放语音。日历点击缩略图→展开小票+播放。列表点击→播放。列表同天多杯→叠放+滑动翻看。
- [x] ~~列表视图~~ → 小票样式卡片，品牌差异化配色，按日期分组
- [x] ~~价格+评分~~ → 新增价格输入 + 1-5星评分
- [x] ~~撕小票动效~~ → 记录完成 → 小票预览 → 撕下动画 → 存入
- [ ] 品牌预设列表是否完整？需要增减哪些？
- [ ] 小料列表是否完整？
- [ ] 杯型选项是否需要调整？
- [ ] 小票各品牌配色方案是否满意？
- [ ] 是否有其他你想到但没提到的需求？
